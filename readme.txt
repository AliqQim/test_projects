родитель - Mvc.Core_New_Proj

на локалхосте секрет берется из локал-хранилища

а на Azure - из KeyVault.

при чем, как-то это завязано на переменной окружения, в которой хранится URI кейволта. на каком этапе она создается - я не до конца понял: либо при каждом деплое, либо при импорте профиля публикации Azure (App Service - Get publish profile)

и я тупо смотрю, есть ли переменная окружения с указанным именем. если нет - не подключаю ажуровские ~модули

можно бы имя переменной окружения поспецифичнее задать так то

